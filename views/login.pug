extends layout

block content
  link(rel="stylesheet" href="/stylesheets/login.css" type="text/css")
  link(rel="stylesheet" href="/stylesheets/misc/alerts.css" type="text/css")

  main.container.pt-4
    .row.justify-content-center
      .col-10.col-md-7.col-lg-5
        h1.h2.mb-4 Šimtadienis

        if failure
          .alert.alert-danger
            | Įvyko techninė klaida.
        if badcreds
          .alert.alert-danger
            | Neteisingas vartotojo vardas/ID arba slaptažodis.
        if logout
          .alert.alert-success
            | Sėkmingai atsijungta.

        form.formContainer(method='post')
          .form-group
            input#username.form-control(type='text' name='username' required)
            label.formLabel(for='username') Vartotojo vardas/ID
          .form-group
            input#password.form-control(type='password' name='password' required)
            label.formLabel(for='password') Slaptažodis
          button.btn.btn-primary(type='submit') Prisijungti
